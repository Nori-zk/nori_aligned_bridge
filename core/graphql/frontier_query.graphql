query FrontierQuery($maxLength: Int!) {
    bestChain(maxLength: $maxLength) {
        stateHash
        protocolState {
            consensusState {
                blockHeight
            }
        }
    }
}
